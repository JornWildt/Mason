<Window x:Class="ApiExplorer.Windows.ComposerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:axuc="clr-namespace:ApiExplorer.MediaTypeHandlers.ApplicationMason.UserControls"
        xmlns:axu="clr-namespace:ApiExplorer.Utilities"
        xmlns:vc="clr-namespace:ApiExplorer.ValueConverters"
        Title="{Binding WindowTitle}" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
  <Grid Margin="10">
    <Grid.RowDefinitions>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="Auto"/>
      <RowDefinition Height="*"/>
      <RowDefinition Height="Auto"/>
    </Grid.RowDefinitions>
    
    <!-- Request line -->
    <Grid Grid.Row="0">
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <ComboBox Grid.Column="0" Width="100" Margin="0 0 4 0" Name="MethodInput"
                Text="{Binding Method}" IsEditable="True"
                ItemsSource="{Binding Methods}" SelectedValuePath="Name" DisplayMemberPath="Name">
      </ComboBox>
      <TextBox Grid.Column="1" Text="{Binding Url}"/>
    </Grid>
    
    <!-- Specific headers -->
    <Grid Grid.Row="1">
      <Grid.RowDefinitions>
        <RowDefinition Height="Auto"/>
      </Grid.RowDefinitions>
      <Grid.ColumnDefinitions>
        <ColumnDefinition Width="Auto"/>
        <ColumnDefinition Width="*"/>
      </Grid.ColumnDefinitions>
      <TextBlock Grid.Row="0" Grid.Column="0" Text="Content-Type" Margin="0 0 4 0" Width="100"/>
      <ComboBox Grid.Row="0" Grid.Column="1" Name="ContentTypeInput" Margin="0 4 0 0" 
                Text="{Binding ContentType}" IsEditable="True">
        <ComboBoxItem Content="application/json"/>
        <ComboBoxItem Content="application/json-patch+json"/>
      </ComboBox>
    </Grid>
    
    <TabControl Grid.Row="2" Margin="0 4 0 0">
      <TabItem Header="Body">
        <!-- Body -->
        <Grid>
          <TextBox Text="{Binding Body}"
                   TextWrapping="NoWrap" AcceptsReturn="True"
                   HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"
                   Visibility="{Binding MethodAllowsContent, Converter={vc:BoolToVisibilityConverter}}"/>
          <TextBlock Text="No content allowed for this method"
                     VerticalAlignment="Center" HorizontalAlignment="Center"
                     Visibility="{Binding MethodAllowsContent, Converter={vc:BoolToVisibilityConverter}, ConverterParameter=InverseVisibility}"/>
        </Grid>
      </TabItem>
      <TabItem Header="Headers">
        <!-- Headers -->
        <TextBox Text="{Binding Headers}"
             TextWrapping="NoWrap" AcceptsReturn="True"
             HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto"/>
      </TabItem>
    </TabControl>
    
    <!-- Buttons -->
    <StackPanel Grid.Row="3" Orientation="Horizontal">
      <Button Content="Execute" Command="{Binding ExecuteCommand}" CommandParameter="{Binding RelativeSource={RelativeSource self}}"
              IsDefault="True" Margin="0 4 0 0"/>
      <Button Content="Cancel" IsCancel="True" Margin="6 4 0 0"/>
    </StackPanel>
  </Grid>
</Window>
